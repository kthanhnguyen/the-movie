import React from "react";

export default function Genres(match, location) {
  // const dispatch = useDispatch();
  // const movieGenre = useSelector((state) => state.movieReducer.movieGenre);
  // const { name } = useParams();

  // useEffect(() => {
  //   dispatch(action.getMovieGenre(name, 1));
  // }, [dispatch]);

  // console.log(name.replace(/\s+/g, "_").toLowerCase());
  // console.log(movieGenre);
  return <div>GenresPage</div>;
}
